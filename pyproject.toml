[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mlx-cache-query"
version = "0.2.0"
description = "Persistent KV caches for local LLMs. Build once, query instantly, pipe everywhere."
requires-python = ">=3.11"
dependencies = [
    "mlx>=0.22.0",
    "mlx-lm>=0.22.0",
    "click>=8.1",
    "pymupdf>=1.24",
    "safetensors>=0.4",
    "rich>=13.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-timeout>=2.2",
]

[project.scripts]
mcq = "mcq.cli:main"

[tool.setuptools.packages.find]
include = ["mcq*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]
